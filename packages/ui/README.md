# Shared UI

This directory is reserved for the suite-wide TypeScript component library used
by Light, Media, and Viz. Product-specific screens and bootstrapping remain in
their respective `apps/*` directories.
