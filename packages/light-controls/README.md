# Light Controls

Shared TypeScript interaction contracts used by more than one Light application belong here.
This package contains behavior and types, not application entry points, server access, Tauri
bootstrapping, or product-owned state.

`programmer-keypad` is the canonical keypad geometry and OSC action mapping shared by Light
Desktop, Light Hardware Controls, and the root acceptance bench.
