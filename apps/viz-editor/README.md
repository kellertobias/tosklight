# Viz editor

This directory is reserved for the runnable Tauri application used to configure
and manage Viz. Its entry points, desktop lifecycle, and application-specific
web interface belong here.

Reusable visualization behavior belongs in `crates/viz`, while the output
process belongs in `apps/viz-renderer`.
