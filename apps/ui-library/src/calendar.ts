/**
 * Future Scheduler feature prototype.
 * Intentionally retained for Storybook product design and later production wiring;
 * do not classify this entry point as dead code while Scheduler is not yet shipped.
 */
export * from "./calendar/Calendar";
