# Viz renderer

This directory is reserved for the runnable Rust visualization renderer that
shows the configured scene. Keep process startup and other bootstrapping here;
reusable visualization behavior belongs in `crates/viz`.
