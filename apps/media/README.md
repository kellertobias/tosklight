# Media application

This directory is reserved for the runnable Media application: its Rust server
entry point and the web interface that it serves. Media is a web application,
not a Tauri application.

Reusable Media behavior belongs in `crates/media`. Shared TypeScript components
belong in `packages/ui`.
