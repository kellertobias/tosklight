# Default Attribute Activation and Encoder Assignments

> Generated from `AttributeConfiguration::recommended()` by `cargo run --package light-core --example generate_attribute_reference`. Do not edit this file by hand.

These are the defaults applied by **Restore recommended defaults**. A show may have a different layout or activation grouping.

## Default encoder assignments

| Encoder group | Encoder | Attribute | Push-turn companion |
| --- | --- | --- | --- |
| Intensity | P1/E1 | **Intensity** (`intensity`) | — |
| Intensity | P1/E2 | **Shutter / Strobe** (`shutter`) | — |
| Intensity | P1/E3 | **Mask Opacity** (`media.mask.opacity`) | — |
| Intensity | P1/E4 | **Volume** (`volume`) | — |
| Intensity | P1/E5 | **Audio Volume** (`audio.volume`) | — |
| Color | P1/E1 | **Red** (`color.red`) | — |
| Color | P1/E2 | **Green** (`color.green`) | — |
| Color | P1/E3 | **Blue** (`color.blue`) | — |
| Color | P1/E4 | **White** (`color.white`) | — |
| Color | P1/E5 | **Amber** (`color.amber`) | — |
| Color | P1/E6 | **UV** (`color.uv`) | — |
| Color | P2/E1 | **Lime** (`color.lime`) | — |
| Color | P2/E2 | **Indigo** (`color.indigo`) | — |
| Color | P2/E3 | **Mint** (`color.mint`) | — |
| Color | P2/E4 | **Color Temperature** (`color.temperature`) | — |
| Color | P2/E5 | **Color Wheel 1** (`color.wheel.1`) | — |
| Color | P2/E6 | **Color Wheel 2** (`color.wheel.2`) | — |
| Color | P3/E1 | **Color Wheel 1 Rotation** (`color.wheel.1.rotation`) | **Color Wheel 1** (`color.wheel.1`) |
| Color | P3/E2 | **Color Wheel 2 Rotation** (`color.wheel.2.rotation`) | **Color Wheel 2** (`color.wheel.2`) |
| Position | P1/E1 | **Pan** (`pan`) | — |
| Position | P1/E2 | **Tilt** (`tilt`) | — |
| Position | P1/E3 | **Position Movement** (`position.movement`) | — |
| Position | P1/E4 | **Head Rotation** (`position.rotation`) | — |
| Position | P1/E5 | **Position X** (`media.position.x`) | — |
| Position | P1/E6 | **Position Y** (`media.position.y`) | — |
| Position | P2/E1 | **Camera X** (`camera.position.x`) | — |
| Position | P2/E2 | **Camera Y** (`camera.position.y`) | — |
| Position | P2/E3 | **Camera Z** (`camera.position.z`) | — |
| Position | P2/E4 | **Camera Yaw** (`camera.yaw`) | — |
| Position | P2/E5 | **Camera Pitch** (`camera.pitch`) | — |
| Position | P2/E6 | **Camera Roll** (`camera.roll`) | — |
| Position | P3/E1 | **Camera Zoom** (`camera.zoom`) | — |
| Position | P4/E1 | **Point X** (`point.position.x`) | — |
| Position | P4/E2 | **Point Y** (`point.position.y`) | — |
| Position | P4/E3 | **Point Z** (`point.position.z`) | — |
| Position | P4/E4 | **Point Rot X** (`point.rotation.x`) | — |
| Position | P4/E5 | **Point Rot Y** (`point.rotation.y`) | — |
| Position | P4/E6 | **Point Rot Z** (`point.rotation.z`) | — |
| Beam | P1/E1 | **Gobo 1** (`gobo.1`) | — |
| Beam | P1/E2 | **Gobo 1 Rotation** (`gobo.1.rotation`) | — |
| Beam | P1/E3 | **Gobo 2** (`gobo.2`) | — |
| Beam | P1/E4 | **Gobo 2 Rotation** (`gobo.2.rotation`) | — |
| Beam | P1/E5 | **Prism 1** (`prism.1`) | — |
| Beam | P1/E6 | **Prism 2** (`prism.2`) | — |
| Beam | P2/E1 | **Animation Wheel 1** (`animation.1`) | — |
| Beam | P2/E2 | **Prism 1 Rotation** (`prism.1.rotation`) | **Prism 1** (`prism.1`) |
| Beam | P2/E3 | **Prism 2 Rotation** (`prism.2.rotation`) | **Prism 2** (`prism.2`) |
| Beam | P2/E4 | **Animation Rotation 1** (`animation.1.rotation`) | **Animation Wheel 1** (`animation.1`) |
| Shapers | P1/E1 | **Iris** (`iris`) | — |
| Shapers | P1/E2 | **Blade 1 Position** (`shaper.blade.1.position`) | — |
| Shapers | P1/E3 | **Blade 1 Angle** (`shaper.blade.1.angle`) | — |
| Shapers | P1/E4 | **Blade 2 Position** (`shaper.blade.2.position`) | — |
| Shapers | P1/E5 | **Blade 2 Angle** (`shaper.blade.2.angle`) | — |
| Shapers | P1/E6 | **Shaper Rotation** (`shaper.rotation`) | — |
| Shapers | P2/E1 | **Blade 3 Position** (`shaper.blade.3.position`) | — |
| Shapers | P2/E2 | **Blade 3 Angle** (`shaper.blade.3.angle`) | — |
| Shapers | P2/E3 | **Blade 4 Position** (`shaper.blade.4.position`) | — |
| Shapers | P2/E4 | **Blade 4 Angle** (`shaper.blade.4.angle`) | — |
| Focus | P1/E1 | **Focus** (`focus`) | — |
| Focus | P1/E2 | **Zoom** (`zoom`) | — |
| Focus | P1/E3 | **Softness** (`softness`) | — |
| Control | P1/E1 | **Control** (`control`) | — |
| Control | P1/E2 | **Play Mode** (`media.play_mode`) | — |
| Control | P1/E3 | **Playback Speed** (`media.playback_speed`) | — |
| Control | P1/E4 | **Playback BPM** (`media.playback_bpm`) | — |
| Control | P1/E5 | **Scaling Mode** (`media.scaling_mode`) | — |
| Control | P1/E6 | **Blur** (`media.playback.blur`) | — |
| Media | P1/E1 | **Media Folder** (`media.folder`) | — |
| Media | P1/E2 | **Media File** (`media.file`) | — |
| Media | P1/E3 | **Mask Folder** (`media.mask.folder`) | — |
| Media | P1/E4 | **Mask File** (`media.mask.file`) | — |
| Media | P1/E5 | **Invert Mask** (`media.mask.invert`) | — |
| Media | P1/E6 | **Flip / Mirror** (`media.flip_mirror`) | — |
| Media | P2/E1 | **Audio Folder** (`audio.folder`) | — |
| Media | P2/E2 | **Audio File** (`audio.file`) | — |
| Media | P2/E3 | **Audio Transport** (`audio.transport`) | — |
| Media | P2/E4 | **Audio Repeat** (`audio.repeat`) | — |
| Media | P3/E3 | **Scale X** (`media.scale.x`) | — |
| Media | P3/E4 | **Scale Y** (`media.scale.y`) | — |
| Media | P4/E1 | **Media Effect 1** (`media.effect.1`) | — |
| Media | P4/E2 | **Media Effect 2** (`media.effect.2`) | — |
| Media | P4/E3 | **Media Effect 3** (`media.effect.3`) | — |
| Media | P4/E4 | **Media Effect 4** (`media.effect.4`) | — |
| Media | P5/E1 | **Mask Scale X** (`media.mask.scale.x`) | — |
| Media | P5/E2 | **Mask Scale Y** (`media.mask.scale.y`) | — |
| Media | P5/E3 | **Mask Position X** (`media.mask.position.x`) | — |
| Media | P5/E4 | **Mask Position Y** (`media.mask.position.y`) | — |

## Default attribute activation groups

| Group | Attributes activated together |
| --- | --- |
| **Color Mix** | **Color** (`color`)<br>**Red** (`color.red`)<br>**Green** (`color.green`)<br>**Blue** (`color.blue`)<br>**Amber** (`color.amber`)<br>**White** (`color.white`)<br>**UV** (`color.uv`)<br>**Lime** (`color.lime`)<br>**Indigo** (`color.indigo`)<br>**Mint** (`color.mint`)<br>**Color Temperature** (`color.temperature`) |
| **Color Wheel 1** | **Color Wheel 1** (`color.wheel.1`)<br>**Color Wheel 1 Rotation** (`color.wheel.1.rotation`) |
| **Color Wheel 2** | **Color Wheel 2** (`color.wheel.2`)<br>**Color Wheel 2 Rotation** (`color.wheel.2.rotation`) |
| **Position** | **Pan** (`pan`)<br>**Tilt** (`tilt`)<br>**Head Rotation** (`position.rotation`) |
| **Camera** | **Camera X** (`camera.position.x`)<br>**Camera Y** (`camera.position.y`)<br>**Camera Z** (`camera.position.z`)<br>**Camera Yaw** (`camera.yaw`)<br>**Camera Pitch** (`camera.pitch`)<br>**Camera Roll** (`camera.roll`)<br>**Camera Zoom** (`camera.zoom`) |
| **3D Point** | **Point X** (`point.position.x`)<br>**Point Y** (`point.position.y`)<br>**Point Z** (`point.position.z`)<br>**Point Rot X** (`point.rotation.x`)<br>**Point Rot Y** (`point.rotation.y`)<br>**Point Rot Z** (`point.rotation.z`) |
| **Gobo 1** | **Gobo 1** (`gobo.1`)<br>**Gobo 1 Rotation** (`gobo.1.rotation`) |
| **Gobo 2** | **Gobo 2** (`gobo.2`)<br>**Gobo 2 Rotation** (`gobo.2.rotation`) |
| **Media Source** | **Media Folder** (`media.folder`)<br>**Media File** (`media.file`) |
| **Media Mask Source** | **Mask Folder** (`media.mask.folder`)<br>**Mask File** (`media.mask.file`) |
| **Audio Source** | **Audio Folder** (`audio.folder`)<br>**Audio File** (`audio.file`) |
| **Shapers** | **Iris** (`iris`)<br>**Blade 1 Position** (`shaper.blade.1.position`)<br>**Blade 1 Angle** (`shaper.blade.1.angle`)<br>**Blade 2 Position** (`shaper.blade.2.position`)<br>**Blade 2 Angle** (`shaper.blade.2.angle`)<br>**Shaper Rotation** (`shaper.rotation`)<br>**Blade 3 Position** (`shaper.blade.3.position`)<br>**Blade 3 Angle** (`shaper.blade.3.angle`)<br>**Blade 4 Position** (`shaper.blade.4.position`)<br>**Blade 4 Angle** (`shaper.blade.4.angle`) |
| **Intensity** | **Intensity** (`intensity`) |
| **Shutter / Strobe** | **Shutter / Strobe** (`shutter`) |
| **Volume** | **Volume** (`volume`) |
| **Position Movement** | **Position Movement** (`position.movement`) |
| **Prism 1** | **Prism 1** (`prism.1`) |
| **Prism 1 Rotation** | **Prism 1 Rotation** (`prism.1.rotation`) |
| **Prism 2** | **Prism 2** (`prism.2`) |
| **Prism 2 Rotation** | **Prism 2 Rotation** (`prism.2.rotation`) |
| **Animation Wheel 1** | **Animation Wheel 1** (`animation.1`) |
| **Animation Rotation 1** | **Animation Rotation 1** (`animation.1.rotation`) |
| **Focus** | **Focus** (`focus`) |
| **Zoom** | **Zoom** (`zoom`) |
| **Softness** | **Softness** (`softness`) |
| **Audio Transport** | **Audio Transport** (`audio.transport`) |
| **Audio Repeat** | **Audio Repeat** (`audio.repeat`) |
| **Audio Volume** | **Audio Volume** (`audio.volume`) |
| **Play Mode** | **Play Mode** (`media.play_mode`) |
| **Playback Speed** | **Playback Speed** (`media.playback_speed`) |
| **Playback BPM** | **Playback BPM** (`media.playback_bpm`) |
| **Blur** | **Blur** (`media.playback.blur`) |
| **Scaling Mode** | **Scaling Mode** (`media.scaling_mode`) |
| **Position X** | **Position X** (`media.position.x`) |
| **Position Y** | **Position Y** (`media.position.y`) |
| **Scale X** | **Scale X** (`media.scale.x`) |
| **Scale Y** | **Scale Y** (`media.scale.y`) |
| **Mask Position X** | **Mask Position X** (`media.mask.position.x`) |
| **Mask Position Y** | **Mask Position Y** (`media.mask.position.y`) |
| **Mask Scale X** | **Mask Scale X** (`media.mask.scale.x`) |
| **Mask Scale Y** | **Mask Scale Y** (`media.mask.scale.y`) |
| **Mask Opacity** | **Mask Opacity** (`media.mask.opacity`) |
| **Invert Mask** | **Invert Mask** (`media.mask.invert`) |
| **Flip / Mirror** | **Flip / Mirror** (`media.flip_mirror`) |
| **Media Effect 1** | **Media Effect 1** (`media.effect.1`) |
| **Media Effect 2** | **Media Effect 2** (`media.effect.2`) |
| **Media Effect 3** | **Media Effect 3** (`media.effect.3`) |
| **Media Effect 4** | **Media Effect 4** (`media.effect.4`) |
