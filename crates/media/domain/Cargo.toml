[package]
name = "media-domain"
version.workspace = true
edition.workspace = true
license-file.workspace = true
authors.workspace = true

# The Media domain depends only on the standard library plus small value and serialization
# crates. Protocol, HTTP, filesystem, decoder, GPU, and operating-system types never enter
# domain state; adapters translate those at the boundary.
[dependencies]
serde.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
