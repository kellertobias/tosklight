# Viz behavior

This directory is reserved for reusable visualization domain and application
logic shared by the Viz editor and renderer. Runnable entry points belong in
`apps/viz-editor` and `apps/viz-renderer`.
